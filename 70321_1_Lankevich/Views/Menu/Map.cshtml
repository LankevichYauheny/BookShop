@model IEnumerable<_70321_1_Lankevich.Models.MenuItem>

<h6>Карта сайта</h6>
<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.Name, item.Action, item.Controller)</li>
    }
    
</ul>
